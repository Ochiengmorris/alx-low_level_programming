#include "main.h"

/**
 * add - adds two integers and returns the result
 * @d: int to be added to d
 * @e: int to be added to e
 * Return: sum of d and e
 */
int add(int d, int e)
{
int f;

f = d + e;
return (f);
}
