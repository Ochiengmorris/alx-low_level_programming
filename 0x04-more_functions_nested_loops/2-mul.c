#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply 2 integers.
 * @c: An integer to be multiplied with b
 * @d : An integer to be multipled with a
 * Return: Return the result of the multiplication
 */
int mul(int c, int d)
{
	return (c * d);
}
