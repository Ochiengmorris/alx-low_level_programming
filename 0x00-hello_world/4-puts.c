#include <stdio.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char Ownstr[] = "\"Programming is like building a multilingual puzzle";

	puts(Ownstr);

	return (0);
}
